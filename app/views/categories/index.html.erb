<h3>All Categories</h3>

<% @categories.each do |category| %>
  <ul class=category_<%= category.id %>>
    <%= link_to "#{category.title} (#{category.jobs.count})", category_path(category), {:class=>"light_link"} %><br>
  </ul>
<% end %>
<br> 
<%= link_to "Create a Category", new_category_path %>
