<h3 class="text-center"><%= link_to "Categories", categories_path %></h3>
<h1><%= @category.title %></h1>
<%= render partial: "form" %>
<p>Jobs with  <%= @category.title %> Category</p>

<%= @category.jobs.empty? ? "No Jobs are assigned to this category" : @format  %>
