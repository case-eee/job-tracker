<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
  </head>
  <header>
    <% if @comment.errors.any? %>
    <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this record from being saved:</h2>
    <ul><% @comment.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    <% end %>
  </header>
  <body>
    <%= form_for([@job.company, @job, @comment]) do |f| %>
    <form class="form-horizontal">
      <div class="form-group">
        <label for="inputEmail3" class="col-sm-2 control-label">Content</label>
        <div class="col-sm-10">
          <textarea class="form-control" rows="3" name="comment[content]" id="comment_content"></textarea>
        </div>
      </div>
      <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
          <button type="submit" class="btn btn-primary">Submit</button>
        </div>
      </div>
    <% end %>
  </body>
</html>
