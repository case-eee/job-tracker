<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <header>
    <%= @job.title %> at <%= @job.company.name %>
  </header>
  <body class="job_comments">
    <% @comments.each do |comment| %>
    <%= comment.content %>
    <%= link_to "Edit", edit_company_job_comment_path(@job.company, @job, comment) %>
    <% end %>
  </body>
</html>
