<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    <header>
    </header>
  </head>
  <body id="contact_form">
    <%= form_for [@company, @contact] do |f| %>
    <% if @contact.errors.any? %>
    <h2><%= pluralize(@contact.errors.count, "error") %> prohibited this record from being saved:</h2>
    <ul><% @contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
    <% end %>
    <form class="form-horizontal">
      <div class="form-group">
        <%= f.label :name %>
        <div >
          <%= f.text_field :name %>
        </div>
      </div>
      <div class="form-group">
        <%= f.label :position %>
        <div >
          <%= f.text_field :position %>
        </div>
      </div>
      <div class="form-group">
        <%= f.label :email %>
        <div class=>
          <%= f.email_field :email %>
        </div>
      </div>
      <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
          <button type="submit" class="btn btn-primary">Submit</button>
        </div>
      </div>
      <% end %>
    </form>
  </body>
</html>
