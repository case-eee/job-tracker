<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
  </head>
  <body>
    <h1>Company: <%= @company.name %></h1>
    <ul>
      <li>Name: <%= @contact.name %></li>
      <li>Position: <%= @contact.position %></li>
      <li>Email: <%= @contact.email %></li>
      <%= link_to "Home", company_contacts_path(@company) %>
      <%= link_to "Edit", edit_company_contact_path(@company, @contact) %>
    </ul>
  </body>
</html>
