<% if @error %>
  <em><strong><%= @error %></strong></em>
<% end %>

<div class='container'>
    <h2>Create a new job at <%= @company.name %> here!</h2>
    <p>Need to create a Category? <%= link_to "Make one here!", new_category_path %>
</div>
<%= render partial: "form" %>
