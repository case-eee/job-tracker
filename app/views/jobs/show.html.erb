<%= link_to "Back to Jobs for #{@company.name}", company_path(@company) %><br />

<h1>Title: <%= @job.title %></h1>
<br>

Description: <%= @job.description %>
<br>

Level of Interest: <%= @job.level_of_interest %>
<br>

City: <%= @job.city %>
<br>

Category: <%= link_to @job.category.title, category_path(@job.category) %>
<br>

Company: <%= link_to @job.company.name, company_path(@job.company) %>
<br />

<%= link_to "Edit", edit_company_job_path(@company, @job) %><br />
<%= link_to "Delete", company_job_path(@company, @job), method: :delete, data: {confirm: "Are you sure you want to delete this job?"} %>
<br />
<br />

Comments:<br />

<% if @job.comments.count > 0 %>
  <ul>
    <% @job.comments.each do |comment| %>
    <li>
      <%= comment.content %>
      <%= link_to "Edit", edit_company_job_comment_path(@company, @job, comment) %>
      |
      <%= link_to "Delete", company_job_comment_path(@company, @job, comment), method: :delete, data: {confirm: "Are you sure you want to delete this comment?"} %>
    </li>
    <% end %>
  </ul>
  <br />
<% else %>
  <%= "No comments" %>
<% end %>


<%= link_to "Add Comment", new_company_job_comment_path(@company, @job) %>
