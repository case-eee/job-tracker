<h1 class="title">Title: <%= @job.title %></h1>
<br>
<div class="index_body">
Description: <%= @job.description %>
<br>

Level of Interest: <%= @job.level_of_interest %>
<br>

City: <%= @job.city %>
<br>
</div>
<h3><%= link_to "Delete", company_job_path(@company,@job), method: :delete, data: {confirm: "Arse you sure you want to delete this job?" } %></h3>
<h3>Comments</h3>
<%= render partial: 'comments/form' %>
<%= render partial: 'jobs/comment', collection: @comments %>
<%= link_to "Back to #{@job.company.name}", company_path(@company) %>
