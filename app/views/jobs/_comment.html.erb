<div class="row">
  <div class="col-md-offset-5 col-md-4 comments">
    <div class="comment_#{<%= comment.id %>} commentContainer">
      <p class="comment"><%= comment.content %></p>
      <p class = "texter">Posted <%= distance_of_time_in_words(comment.job.created_at, comment.created_at, include_seconds: true) %> later </p>
    </div>
  </div>
</div>
