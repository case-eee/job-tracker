<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% if @company.errors.any? %>
    <h2><%= pluralize(@company.errors.count, "error") %> prohibited this record from being saved:</h2>
    <ul><% @company.errors.full_messages.each do |message| %>
      <li><%= message %></li>
          <% end %>
      <% end %>
      <%= form_for @company do |f| %>
      <form class="form-horizontal">
        <div class="form-group">
          <label for="inputName" class="col-sm-2 control-label"> <%= f.label :name %></label>
          <div class="col-sm-10">
            <input type="name" class="form-control" id="inputName" placeholder= <%= f.text_field :name %>
          </div>
        </div>
        <div class="form-group">
          <div class="col-sm-offset-2 col-sm-10">
            <button type="submit" class="btn btn-default">Submit</button>
          </div>
        </div>
      </form>
      <% end %>
  </body>
</html>
