 <div class="form-group">

 <% if @company.errors.any? %>
    <h2><%= pluralize(@company.errors.count, "error") %> prohibited this record from being saved:</h2>
    <ul>
      <% @company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>

<%= form_for @company do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit %>
<% end %>
</div>
