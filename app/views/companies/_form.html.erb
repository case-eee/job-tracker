<div class="container">
  <div class="col-md-6">
    <%= bootstrap_form_for(@company) do |f| %>
      <%= f.text_field :name %>
      <%= f.submit nil, class: "btn btn-default"%>
    <% end %>
  </div>
</div>